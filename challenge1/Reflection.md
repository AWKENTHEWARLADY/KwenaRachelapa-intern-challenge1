
# Reflection

# Hardest part ideas
detecting data leakage hidden in features
fixing TF-IDF without breaking pipeline flow
tracing why model failed after deployment
linking dataset design to model behavior


# What i struggled with
understanding feature importance output
separating real learning signals from shortcuts


# How you solved it
inspected raw dataset first
removed suspicious features
rebuilt train/test workflow correctly


# What i  would improve
test multiple models beyond Random Forest
add better feature engineering for text
handle class imbalance more carefully

# What i have learned
data leakage can inflate performance silently
preprocessing order changes model correctness
accuracy alone does not measure quality
feature importance reveals hidden problems
real ML work depends more on data than model choice